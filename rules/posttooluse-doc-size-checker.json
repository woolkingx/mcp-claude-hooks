{
  "name": "posttooluse-doc-size-checker",
  "description": "Warn when documentation files exceed character count thresholds (Claude Code MAX_MEMORY_CHARACTER_COUNT = 40000 per file)",
  "enabled": true,
  "event": "PostToolUse",
  "priority": 60,
  "tool": [
    "Edit",
    "Write"
  ],
  "action": "context",
  "match": {
    "properties": {
      "tool_input": {
        "properties": {
          "file_path": {
            "type": "string",
            "pattern": "(CLAUDE|AGENTS|GEMINI|claude|agents|gemini)\\.md$"
          }
        }
      }
    }
  },
  "feature": {
    "name": "doc-size-checker",
    "config": {
      "unit": "lines",
      "best": 200,
      "warn": 300,
      "error": 500
    }
  }
}
